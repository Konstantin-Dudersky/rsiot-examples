import { Input } from "global/input.slint";

export component MainWindow inherits Window {
    width: 800px;
    height: 480px;
    title: "My Application";
    default-font-size: 14px;

    Text {
        text: "Главное окно";
    }

    Image {
        source: Input.svg_image;
    }
}
